version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: dpanel:latest
    ports:
      - 80:80
    restart: always
    network_mode: bridge
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - .:/app # necessary for development
